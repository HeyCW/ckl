# Core dependencies
Pillow==9.5.0
ttkthemes>=3.2.0       # Tema tambahan untuk ttk

# Database (Postgres primary, SQLite fallback - see .env.example)
psycopg[binary,pool]>=3.1
python-dotenv>=1.0.0

# Password hashing
bcrypt>=4.0.0

# Development dependencies (optional)
pytest>=6.0.0
black>=21.0.0
flake8>=3.8.0

pandas
openpyxl

pyinstaller

reportlab